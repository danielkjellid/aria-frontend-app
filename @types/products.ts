/* eslint-disable no-shadow */
/* eslint-disable import/prefer-default-export */

// NOTE: Keep these and ProductStatus enum in backend in sync.
export enum ProductStatusEnum {
  Draft = 1,
  Hidden = 2,
  Available = 3,
  Discontinued = 4,
}
